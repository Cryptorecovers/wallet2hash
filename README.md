# wallet2hash
Wallet2Hash is an offline tool that detects supported cryptocurrency wallet files and extracts/converts the required hash for password recovery with Hashcat or John the Ripper. When both formats are supported, you can choose the desired output automatically.
